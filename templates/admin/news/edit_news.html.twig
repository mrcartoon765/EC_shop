
    {% include "common/header_admin.html.twig" %}
	<main>
        <div class="wrapper">
            <div class="container">
                <div class="wrapper-title">
                    <h3>編集</h3>
                </div>
                <form class="edit-form" action="{{constant('config\\Bootstrap::ENTRY_URL')}}/update_news.php" method="POST">
                    <input type="hidden" name="id" value={{news[0]['id']}}>
                    <div class="form-group">
                        <p>タイトル</p>
                        <input class="border border-4 border-info" type="text" name="title" value="{{ news[0]['title'] }}" required>
                    </div>
                    <div class="form-group">
                        <p>本文</p>
                        <textarea class="border border-4 border-info" name="content">{{ news[0]['content'] }}</textarea>
                    </div>
                    <button type="submit" class="btn btn-success">更新する</button>
                </form>
            </div>
        </div>
    </main>
 {% include "common/footer.html.twig" %}
    <footer>
        <div class="container">
            <p>Copyright @ 2021  HEALTH KNOWLEGE</p>
        </div>
    </footer>
</body>

</html>