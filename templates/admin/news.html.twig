<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>記事管理</title>

    <link rel="icon" href="favicon.ico">
    <!-- css -->
    <link href="{{constant('config\\Bootstrap::CSS_FILE')}}" rel="stylesheet" >
  
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
        integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
</head>

<body>
    <header>
        <div class="container">
            <div class="header-logo">
                <h1><a href="{{constant('config\\Bootstrap::ENTRY_URL')}}/dashboard.php">管理画面</a></h1>
            </div>

            <nav class="menu-right menu">
                <a href="logout.php">ログアウト</a>
            </nav>
        </div>
    </header>
    <main>
        <div class="wrapper">
            <div class="container">
                <div class="wrapper-title">
                    <h3>記事管理</h3>
                </div>
                <button type="button" class="btn btn-blue" onclick="location.href='create_news.php'">投稿する</button>
                <div class="list">
                    <table>
                        <thead>
                            <tr>
                                <th>id</th>
                                <th>タイトル</th>
                                <th>本文</th>
                                <th>更新日時</th>
                                <th>作成日時</th>
                                <th>操作</th>
                            </tr>
                        </thead>
                        <tbody>
                          {% for value in news %}
                            <tr>
                            <td>{{ attribute(value,'id') }}</td>
                            <td>{{ attribute(value,'title') }}</td>
                            <td>{{ attribute(value,'content') }}</td>
                            <td>{{ attribute(value,'updated_at') }}</td>
                            <td>{{ attribute(value,'created_at') }}</td>
                            <td><button type="button" class="btn btn-green" onclick="location.href='{{ constant('config\\Bootstrap::ENTRY_URL')}}/edit_news.php?id={{ attribute(value,'id') }}'">編集</button>
                            <button type="button" class="btn btn-red">削除</button>
                            </td>
                            </tr>
                          {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
    <footer>
        <div class="container">
            <p>Copyright @ 2018 SQUARE, inc</p>
        </div>
    </footer>
</body>

</html>