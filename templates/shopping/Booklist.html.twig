<!DOCTYPE html>
<html lang="jp">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BookList</title>
</head>
<body>
<div id="wrapper">
{% include 'category_menu.html.twig' %}
<div id="Book_list">
{% for value in dataArr %}
<div class="Book"></div>
<ul>

<li class="title"><a href="{{ constant('shopping\\Bootstrap::ENTRY_URL')}}detail.php?Book_id={{value.Book_id}}">{{ value.title }}</a></a></li>

<li class="price">&yen;{{ value.price|number_format(0, '.', ',') }}</li>

<li class="image">
<a href="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}detail.php?Book_id={{ value.Book_id }}">

{# <img src="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}image/{{ value.image }}" alt="{{ value.title }}"></a></li> #}

<img src =>

<li class="detail"><a href="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}detail.php?Book_id={{ value.Book_id }}"></a>詳細</li>

</ul>
</div>
{% endfor %}
</div>
</div>
</body>
</html>