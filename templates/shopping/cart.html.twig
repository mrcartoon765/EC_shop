<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <div id="wrapper">
  <div id="cart_list">
  <p>ショッピングカート</p>
  <p><a href="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}"list.php>商品一覧へ戻る</a></p>
  <p>カート内商品数：{{ sunmNum }}個 合計金額：&yen; {{ sumPrice|number_format(0, '.', ',') }}</p>
  {% if dataArr | length == 0 %}
  <p>カートに商品は入っていません</p>
  {% else %}
    {% for value in dataArr %}
    <div class="book"></div>
    <ul>
    <li class="image"><img src="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}image/books/{{ value.image }}" alt="{{ value.title }}"></li>
    
    <li class="title">{{ value.title }}</li>
    <li class="price">&yen; {{ value.price|numbert_format(0, '.', ',') }}</li>
    <li><a href="{{ constant('shopping\\Bootstrap::ENTRY_URL') }}cart.php?crt_id={{ value.crt_id }}">削除</a></li>
    </ul>
    </div>
    {% endfor %}
  {% endif %}
  </div>
  </div>
</body>
</html>