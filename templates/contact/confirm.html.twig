<!DOCTYPE html>

{{ header }}

<body>
	{% include "common/header.html.twig" %}
	<main>
		<div class="wrapper last-wrapper">
			<div class="container">
				<div class="wrapper-title">
					<h3>CONFIRM</h3>
					<p>お問い合わせ内容確認</p>
				</div>

				<form method="POST" action="{{APP_URL}}/Contact/send.php" class="conf-form">
					<div class="form-group">
						<p>お名前 *</p>
						<p>{{ name }}</p>
						<input type="hidden" name="name" value="{{ name }}">
						{% if ( name == '') %}
							<p class="error">名前が入力されていません</p>
						{% endif %}
					</p>
				</div>

				<div class="form-group">
					<p>Email *</p>
					<p>{{ email }}</p>
					<input type="hidden" name="email" value="{{ email }}">
					{% if ( email == '') %}
						<p class="error">メールアドレスが入力されていません</p>
					{% endif %}
				</p>
			</div>

			<div class="form-group">
				<p>お問い合わせ内容 *</p>
				<p>{{ text }}</p>
				<input type="hidden" name="text" value="{{ text }}">
				{% if ( text == '') %}
					<p class="error">お問い合わせ内容が入力されていません</p>
				{% endif %}
			</p>
		</div>

		{% if 'name' != '' and 'email' !='' and 'text' !='' %}
			<p>この内容で送信してよろしいですか？</p>
			<button type="submit" class="btn btn-submit">送信する</button>
		{% else %}
			<button class="btn btn-reutrn">戻る</button>
		{% endif %}
	</form>
</body></div></div></main>
 {% include "common/footer.html.twig" %}</body>{{ footer }}</html>
